---
platform: linux

image_resource:
  type: docker-image
  source:
    #repository: brianbyers/concourse-cfcli
    repository: {{DOCKER_IMAGE}}
    tag: "latest"


inputs:
  - name: git-repo

run:
  path: git-repo/ci/tasks/verify-test.sh

params:
  DOCKER_IMAGE:
  CONCOURSE_DEMO_URL:
