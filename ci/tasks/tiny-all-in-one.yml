---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ((docker_image))
    tag: latest

inputs:
  - name: resource-git

outputs:
  - name: artifact-dir

run:
  path: sh
  args:
  - -exc
  - |
    echo "hello"
    echo ${docker_image}
  #path: resource-git/ci/tasks/tiny-all-in-one.sh

params:
  docker_image: