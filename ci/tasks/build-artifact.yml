---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: {{DOCKER_IMAGE}}
    tag: "latest"


inputs:
  - name: git-repo
  - name: version

outputs:
  - name: artifact-dir

run:
  path: git-repo/ci/tasks/build-artifact.sh
